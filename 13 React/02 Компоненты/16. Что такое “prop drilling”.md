# Что такое prop drilling?

> [!summary]
> Prop drilling - это ситуация, когда props приходится передавать через несколько промежуточных компонентов, которым эти данные не нужны. Сам по себе prop drilling не всегда плох, но при глубокой вложенности он делает код шумным и хрупким.

## Пример prop drilling

```jsx
function App() {
  const user = { name: 'Анна' }

  return <Layout user={user} />
}

function Layout({ user }) {
  return <Sidebar user={user} />
}

function Sidebar({ user }) {
  return <UserMenu user={user} />
}

function UserMenu({ user }) {
  return <p>{user.name}</p>
}
```

`Layout` и `Sidebar` просто передают `user` дальше, хотя сами его не используют.

## Почему это может быть проблемой?

- Промежуточные компоненты знают о лишних данных.
- При переименовании prop нужно менять много мест.
- Сложнее перестраивать дерево.
- Компоненты становятся менее переиспользуемыми.
- Список props разрастается.

## Когда это нормально?

Если глубина небольшая, prop drilling может быть самым простым и понятным решением:

```jsx
<UserCard user={user} onSelect={handleSelect} />
```

Не нужно подключать глобальное состояние только ради передачи на один уровень ниже.

## Как решать?

Варианты:

- композиция через `children`;
- поднять или опустить state;
- React Context;
- custom hooks;
- state manager;
- colocate state ближе к месту использования.

Пример с composition:

```jsx
function App() {
  return (
    <Layout
      sidebar={<UserMenu user={user} />}
    />
  )
}
```

Теперь `Layout` не обязан знать про `user`.

## Мини-шпаргалка

- Prop drilling - передача props через ненужные промежуточные уровни.
- Небольшой prop drilling - нормально.
- Проблема начинается с шума и хрупкости.
- Context не нужен для каждого prop.
- Часто помогает композиция или state colocation.

## Связанные темы

- [[11. Что такое композиция компонентов]]
- [[20. Что делает useContext()]]
- [[18. Что делает “state colocation”]]
