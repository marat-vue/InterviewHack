# Как создать Error Boundary компонент вручную?

> [!summary]
> Error Boundary вручную создают как class component с методами `static getDerivedStateFromError` и `componentDidCatch`. Первый переключает UI на fallback, второй подходит для логирования ошибки и component stack.

## Минимальная версия

```jsx
import React from 'react'

class ErrorBoundary extends React.Component {
  state = {
    hasError: false,
    error: null,
  }

  static getDerivedStateFromError(error) {
    return {
      hasError: true,
      error,
    }
  }

  componentDidCatch(error, errorInfo) {
    console.error(error, errorInfo.componentStack)
  }

  render() {
    if (this.state.hasError) {
      return <h2>Something went wrong</h2>
    }

    return this.props.children
  }
}
```

Использование:

```jsx
<ErrorBoundary>
  <UserWidget />
</ErrorBoundary>
```

Если `UserWidget` упадет при render, boundary покажет запасной UI.

## Версия с fallback prop

```jsx
class ErrorBoundary extends React.Component {
  state = {
    hasError: false,
    error: null,
  }

  static getDerivedStateFromError(error) {
    return { hasError: true, error }
  }

  componentDidCatch(error, errorInfo) {
    this.props.onError?.(error, errorInfo)
  }

  reset = () => {
    this.setState({ hasError: false, error: null })
    this.props.onReset?.()
  }

  render() {
    if (this.state.hasError) {
      if (this.props.fallback) {
        return this.props.fallback
      }

      if (this.props.fallbackRender) {
        return this.props.fallbackRender({
          error: this.state.error,
          reset: this.reset,
        })
      }

      return (
        <div role="alert">
          <p>Something went wrong.</p>
          <button onClick={this.reset}>Try again</button>
        </div>
      )
    }

    return this.props.children
  }
}
```

Использование:

```jsx
<ErrorBoundary
  onError={(error, info) => reportError(error, info)}
  fallbackRender={({ error, reset }) => (
    <div role="alert">
      <p>{error.message}</p>
      <button onClick={reset}>Retry</button>
    </div>
  )}
>
  <PaymentsWidget />
</ErrorBoundary>
```

## Где ставить boundary?

```jsx
function App() {
  return (
    <>
      <Header />

      <ErrorBoundary fallback={<MainContentError />}>
        <MainRoutes />
      </ErrorBoundary>

      <ErrorBoundary fallback={<ChatUnavailable />}>
        <SupportChat />
      </ErrorBoundary>
    </>
  )
}
```

Граница должна соответствовать смысловому блоку UI.

## Библиотека react-error-boundary

В реальном проекте часто используют готовую библиотеку:

```jsx
import { ErrorBoundary } from 'react-error-boundary'

<ErrorBoundary
  fallbackRender={({ error, resetErrorBoundary }) => (
    <div role="alert">
      <p>{error.message}</p>
      <button onClick={resetErrorBoundary}>Try again</button>
    </div>
  )}
  onError={reportError}
>
  <Profile />
</ErrorBoundary>
```

Она дает удобный API для reset, fallback render и интеграции с functional components.

## Мини-шпаргалка

- Error Boundary вручную - class component.
- `getDerivedStateFromError` нужен для fallback UI.
- `componentDidCatch` нужен для логирования.
- Boundary ловит ошибки только ниже себя.
- Для production удобна библиотека `react-error-boundary`.

## Связанные темы

- [[4. Что такое Error Boundary]]
- [[6. Какие типы ошибок ловит Error Boundary]]
- [[10. Логирование ошибок]]
