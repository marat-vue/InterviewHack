# Можно ли использовать fetch напрямую в Server Component?

> [!summary]
> Да, в App Router это один из основных паттернов. Server Component может быть `async`, вызывать `await fetch`, использовать server env и управлять cache через options `cache`, `next.revalidate` и `next.tags`.

## Базовый пример

```tsx
export default async function PostsPage() {
  const response = await fetch('https://api.example.com/posts')

  if (!response.ok) {
    throw new Error('Failed to load posts')
  }

  const posts = await response.json()

  return <PostList posts={posts} />
}
```

## С server env

```tsx
await fetch('https://api.example.com/private', {
  headers: {
    Authorization: `Bearer ${process.env.API_TOKEN}`,
  },
})
```

Такой код должен оставаться на сервере.

## С cache options

```tsx
await fetch(url, { cache: 'force-cache' })
await fetch(url, { cache: 'no-store' })
await fetch(url, { next: { revalidate: 3600 } })
await fetch(url, { next: { tags: ['posts'] } })
```

## Когда не стоит?

Не стоит делать прямой `fetch` в Server Component, если:

- это mutation;
- нужен публичный HTTP endpoint;
- запрос зависит от client-only state;
- данные должны обновляться после каждого keystroke;
- нужна сложная client cache логика.

## Что отвечать на собеседовании?

В Server Component можно использовать `await fetch` напрямую. Запрос выполнится на сервере, не попадет в client bundle и может использовать Next.js cache options. Для client interaction или live refetch лучше Client Component и data library.

## Частые ошибки

- Не проверять `response.ok`.
- Делать mutation через GET-запрос в render.
- Создавать лишний Route Handler для внутреннего server fetch.
- Передавать секретные данные клиенту.
- Использовать `fetch` без продуманной cache strategy.

## Мини-шпаргалка

- Server Component может быть `async`.
- `fetch` выполняется на сервере.
- Можно использовать env secrets.
- Проверяй `response.ok`.
- Cache задается options.
- Mutations делай через Server Actions/Route Handlers.

## Связанные темы

- [[1. Как Next.js загружает данные на сервере]]
- [[8. Какие опции кэширования есть у fetch]]
- [[5. Можно ли делать await fetch прямо в компоненте]]
- [[8. Что такое API Routes в Next.js]]
- [[1. Что такое Server Actions в Next.js]]
