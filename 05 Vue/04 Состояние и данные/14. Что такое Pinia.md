# Что такое Pinia

> [!summary] Коротко
> Pinia - официальный менеджер состояния для Vue. Он позволяет создавать stores с общим state, getters и actions, которые доступны разным компонентам приложения.

## Вопрос

Что такое Pinia?

## Базовый store

```typescript
import { defineStore } from "pinia";

export const useCounterStore = defineStore("counter", {
  state: () => ({
    count: 0,
  }),
  getters: {
    doubled: (state) => state.count * 2,
  },
  actions: {
    increment() {
      this.count++;
    },
  },
});
```

## Использование в компоненте

```vue
<script setup>
import { useCounterStore } from "@/stores/counter";

const counter = useCounterStore();
</script>

<template>
  <button @click="counter.increment()">
    {{ counter.doubled }}
  </button>
</template>
```

## Что хранить в Pinia

Pinia подходит для состояния, которое нужно разным частям приложения: пользователь, корзина, permissions, общие справочники, настройки.

## Что не хранить

Локальные поля формы, флаг открытия маленького dropdown-а или временные UI-состояния обычно лучше держать в компоненте.

## Мини-шпаргалка

- Pinia - state manager для Vue.
- Store содержит state, getters и actions.
- `defineStore` создает store.
- Компоненты получают store через composable вида `useXStore`.
- Pinia стоит использовать для общего состояния, а не для всего подряд.

## Связанные темы

- [[Что такое менеджер состояния]]
- [[Почему Pinia заменяет Vuex в экосистеме Vue]]
- [[Почему в Pinia нет мутаций]]
